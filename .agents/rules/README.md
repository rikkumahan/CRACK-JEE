# Modular rules (Antigravity)
Antigravity reads root AGENTS.md automatically (v1.20.3+). This folder is for
extra workspace rules. `.agents/rules/` is current; `.agent/rules/` legacy.
Each rule file max 12,000 chars.
